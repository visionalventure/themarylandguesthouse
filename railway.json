{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '[diag] pwd='$(pwd) && echo '[diag] root:' && ls && echo '[diag] apps/api/dist:' && ls apps/api/dist/ 2>&1 || true && echo '[diag] dist:' && ls dist/ 2>&1 || true && echo '[diag] done, sleeping' && sleep 3600",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
