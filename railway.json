{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema ./prisma/schema.prisma && echo '[shell] migration done, node version:' && node --version && echo '[shell] dist/main.js exists:' && ls -lh dist/main.js && echo '[shell] starting node...' && node dist/main || echo '[shell] node exited with code' $?",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
