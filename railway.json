{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '[diag] ls dist:' && ls dist/ 2>&1 && echo '[diag] node version:' && node --version && echo '[diag] starting...' && node dist/main 2>&1; echo '[diag] exited:' $?",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
